enum Gender {
  male,
  female,
}

enum AccessibilityStaysType {
  active,
  notApproved,
  notRegistered,
  notVerified,
  inActive,
  blocked,
  isRejected,
}

enum StudentLocation {
  mazzeh,
  hamak,
  barzeh,
}

enum OrderStatus {
  pending,
  approved,
  notApproved,
  prepared,
  failedAssigning,
  picked,
  delivered,
  notDelivered,
  canceled,
}
